# Disabled - Mayhem workflow only
name: "CodeQL"
on:
  push:
    branches: [ "disabled-by-mayhem-setup" ]
jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
    - run: echo "disabled"
